В проекте уже используется архитекстура MVVM + Interactor + Coordinator
В проекте реализован уже MVVM, который отвечает за сетевую логику. Он не зависит от ViewController, и не зависит от Coordinator. Выполнен через Interactor. Не совсем догоняю, как его можно улучшить. В проекте уже выполнено все, что было в описании. Менять архитектуру полностью займет пару дней
Как все реализованно в проекте:
    1 - ViewModel — отвечает за подготовку данных для View и взаимодействие с Interactor.
    2 - Interactor — содержит бизнес-логику, включая сетевые запросы.
    3 - Coordinator — управляет навигацией и потоком данных между экранами.
    4 - View (ViewController) — отвечает за отображение данных.
    
Да, конечно, можно убрать Interactor, и сделать весь код без него. Но тут у меня уйдет неделя времени. За день я не смогу переписать во всем проекте логику. Также я убрал бы полностью Сториборды и сделал бы все элементы кодом, так как сториборды тяжело понимать при большом интерфейсе и могут возникнуть ошибки при работе с гитом в сторибордах. Ну это тоже долгая история, не справлюсь за 1 день.
    
Реализованно не плохо. Максимум что можно улучшить, попытаться убрать все системные ошибки. В проекте также стоит улучшить оптимизацию переиспользования ячеек

Добавление аналитики:
    Во ViewModel -> Для событий, связанных с бизнес логикой
    В Coordinator -> Для событий, связанных с навигацией
    Во ViewController -> Для отслеживания нажатия кнопок

1) Где-то в коде, таблица пытается добавиться на экран до того, как мы получим данные. 
